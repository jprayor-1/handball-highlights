Flask==3.0.0
flask-cors==4.0.0
opencv-python-headless==4.8.1.78
numpy==1.24.3
matplotlib==3.7.2
gunicorn==21.2.0
```

**`Procfile`** (optional but good practice)
```
web: gunicorn app:app --timeout 600 --workers 2 --bind 0.0.0.0:$PORT
```

**`runtime.txt`** (optional - specifies Python version)
```
python-3.11.0
